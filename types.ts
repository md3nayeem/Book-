
export interface OrderDetails {
  fullName: string;
  phoneNumber: string;
  deliveryAddress: string;
  quantity: number;
  district: string;
}

export interface BookCover {
  url: string;
  color: string;
  title: string;
}
